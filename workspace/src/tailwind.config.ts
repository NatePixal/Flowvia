/**
 * This file is a placeholder to resolve a build conflict.
 * The primary configuration is in the root tailwind.config.ts.
 */
import type { Config } from 'tailwindcss'

const config: Config = {
  content: [],
  theme: {
    extend: {},
  },
  plugins: [],
}
export default config
