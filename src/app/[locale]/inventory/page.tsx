// This file's content has been removed to resolve a Next.js build error.
// The error "You cannot have two parallel pages that resolve to the same path"
// was caused by this file and `src/app/[locale]/(app)/inventory/page.tsx` both
// trying to render the /inventory route. The page inside the (app) route
// group is the correct one.
